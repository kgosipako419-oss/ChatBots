# Core dependencies (text mode + brain + skills). No audio required.
requests>=2.31         # Ollama HTTP + ntfy push
PyYAML>=6.0            # config files
psutil>=5.9           # system info / process control
pyautogui>=0.9.54     # media keys, screenshots, typing, window hotkeys
pygetwindow>=0.0.9    # list open windows (ships with pyautogui)
pyperclip>=1.8        # clipboard read/write (ships with pyautogui)
Pillow>=10.0          # screenshots (pyautogui dependency, pinned explicitly)
pycaw>=20240210       # precise Windows volume control
comtypes>=1.2         # pycaw dependency
screen_brightness_control>=0.22  # screen brightness
uiautomation>=2.0     # accessibility tree: read/click UI elements by name (the 'eyes' + 'hands')
rapidocr-onnxruntime>=1.3  # OCR fallback for reading on-screen text
paramiko>=3.4         # SSH to other computers
